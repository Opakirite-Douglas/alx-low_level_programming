#include "main.h"

/**
 * _isdigit - function that checks for digits
 * @c: integer to be checked
 * Return: 1 if c is digit else return 0
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
