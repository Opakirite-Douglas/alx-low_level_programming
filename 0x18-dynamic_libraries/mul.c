#include <stdlib.h>
#include <stdio.h>

/**
 * mul - This func will multiply two int
 * @a: THis represents the first num
 * @b: This represents the second num
 * Return: The product
 */

int mul(int a, int b)
{
	return (a * b);
}
