#include <stdlib.h>
#include <stdio.h>

/**
 * mod - This func will give the remainder two int
 * @a: THis represents the first num
 * @b: This represents the second num
 * Return: The remainder
 */

int mod(int a, int b)
{
	return (a % b);
}
